---
- name: Install basic packages
  hosts: isc
  become: yes
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600 

    - name: Install vim, tmux, and traceroute
      apt:
        name:
          - vim
          - tmux
          - traceroute
        state: present

